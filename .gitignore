# Build output (non escludere build/classes - servono per Docker)
/target/

# Documentazione locale
DIARIO_SVILUPPO.txt

# File temporanei e screenshot
ciao.txt
*.png
*.jpg

# Dump del database (contiene dati sensibili)
*.sql

# Dipendenze scaricabili (opzionale, tenerle semplifica il setup)
# web/WEB-INF/lib/
/build/
